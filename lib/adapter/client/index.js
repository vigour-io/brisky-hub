'use strict'
//change this to the repeating thing or something...
var Hub = require('../../')
module.exports = new Hub({
  properties: {
    connection: true
  }
}).Constructor

// for now just id (a key)
// then instanceId
