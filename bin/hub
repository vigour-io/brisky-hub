#!/usr/bin/env node
'use strict'
// var Config = require('vigour-config')
// var config = new Config()

var files = []
// if (config.files) {
//   config.files.each(function (path) {
//     files.push(require(path.val))
//   })
// }
// this is really nice
var Hub = require('../lib/index')
var hub = new Hub({ //eslint-disable-line
  inject: files
})
